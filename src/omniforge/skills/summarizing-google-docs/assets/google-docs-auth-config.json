Here's a template for the google-docs-auth-config.json file:

```json
{
  "installed": {
    "client_id": "YOUR_CLIENT_ID",
    "project_id": "YOUR_PROJECT_ID",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "YOUR_CLIENT_SECRET",
    "redirect_uris": [
      "urn:ietf:wg:oauth:2.0:oob",
      "http://localhost"
    ]
  }
}
```

To obtain the actual values for client_id, project_id, and client_secret, you'll need to create a project in the Google Cloud Console and enable the Google Docs API. Then, create OAuth 2.0 credentials and download the JSON file, which will contain the actual values to replace in this template.